import { createFileRoute } from '@tanstack/react-router'; import { BursaryDetailScreen } from '@/components/screens';
export const Route=createFileRoute('/bursary/$bursaryId')({head:()=>({meta:[{title:'Opportunity details — BursaryBuddy'},{name:'description',content:'Understand funding coverage, requirements, documents and your match.'},{property:'og:title',content:'Opportunity details — BursaryBuddy'},{property:'og:description',content:'Prepare for your next education funding application.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Page}); function Page(){const{bursaryId}=Route.useParams();return <BursaryDetailScreen id={bursaryId}/>}